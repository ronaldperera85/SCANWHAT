RewriteEngine On

# Muy importante si tu proyecto está en un subdirectorio
# Si SCANWHAT está en la raíz de tu dominio (ej. http://scanwhat.icarosoft.com/),
# puedes comentar o eliminar esta línea.
# Si está en http://localhost/SCANWHAT/, necesitas:
RewriteBase /scanwhat/

# Reglas para evitar bucles y que los archivos existentes se sirvan directamente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# --- REGLAS ESPECÍFICAS PARA TUS PÁGINAS ---

# Para la página de login: /login -> /pages/login.php
RewriteRule ^login/?$ pages/login.php [L,NC,QSA]

# Para la página de registro: /registro -> /pages/registro.php
RewriteRule ^registro/?$ pages/registro.php [L,NC,QSA]

# Para el dashboard (si la raíz y el dashboard son distintos)
# Si index.php ya es tu dashboard cuando estás logueado, esta regla podría no ser necesaria
# o podrías querer una URL como /dashboard
RewriteRule ^dashboard/?$ index.php [L,NC,QSA] # Asumiendo que index.php es el dashboard logueado

# Para "Mi Cuenta": /mi-cuenta -> /pages/mi_cuenta.php (si existe como página completa)
# Si "Mi Cuenta" se carga con AJAX en index.php, este tipo de regla NO es para eso.
# Esta regla es si pages/mi_cuenta.php es un script que genera una página HTML completa.
RewriteRule ^mi-cuenta/?$ pages/mi_cuenta.php [L,NC,QSA]

# Para "Mis Teléfonos": /mis-telefonos -> /pages/mis_telefonos.php
RewriteRule ^mis-telefonos/?$ pages/mis_telefonos.php [L,NC,QSA]

# Para "Desarrolladores": /desarrolladores -> /pages/desarrolladores.php
RewriteRule ^desarrolladores/?$ pages/desarrolladores.php [L,NC,QSA]

# Para "Admin": /admin -> /pages/admin.php
RewriteRule ^admin/?$ pages/admin.php [L,NC,QSA]

# Para "Logout": /logout -> /pages/logout.php
RewriteRule ^logout/?$ pages/logout.php [L,NC,QSA]

# --- REGLA OPCIONAL: Si la raíz del sitio debe ser index.php ---
# Si alguien va a /scanwhat/ (y no coincide con las reglas anteriores)
# y quieres que cargue index.php. Esta suele ser la última regla "catch-all" para la raíz.
# La regla `RewriteRule ^(.*)$ index.php?route=$1` del Front Controller es más general,
# pero si no quieres pasar 'route', puedes usar una más simple para la raíz si
# las demás reglas no la capturan:
# RewriteRule ^$ index.php [L] # Para la raíz exacta del RewriteBase